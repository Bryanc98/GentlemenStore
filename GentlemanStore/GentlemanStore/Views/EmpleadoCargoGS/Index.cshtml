@model IEnumerable<GentlemanStore.Models.EmpleadoCargoGS>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreCargo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreCargo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEmpCargo }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEmpCargo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEmpCargo })
        </td>
    </tr>
}

</table>
